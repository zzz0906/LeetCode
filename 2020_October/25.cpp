class Solution {
public:
    vector<int> square;
    bool winnerSquareGameCounter(int n){
        for (int i = 0; i < square.size(); i++){
            if (square[i] <= n)
        }
    }
    bool winnerSquareGame(int n) {
        for (int i = 0; i <= sqrt(n)+1; i ++)
            if (i*i <= n)
                square.push_back(i*i);
        for (int )
    }
    bool winnerSquareGame(int n) {
        for (int i = 0; i <= sqrt(n)+1; i ++)
            if (i*i <= n)
                square.push_back(i*i);
        for (int )
    }
};